using System;
using System.Collections.Generic;
namespace ToDo
{
    // Enum representing different sizes for a task card   
    public enum Sizes
    {
        XS = 1,
        S = 2,
        M = 3,
        L = 4,
        XL = 5
    }
}